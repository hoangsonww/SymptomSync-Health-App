#!/usr/bin/env bash
# Hook: pre deploy
# Runs automatically during the pre deploy phase
set -euo pipefail

echo "[hook] pre deploy started..."
# TODO: add pre deploy logic
echo "[hook] pre deploy complete."
