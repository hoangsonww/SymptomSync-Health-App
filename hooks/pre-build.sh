#!/usr/bin/env bash
# Hook: pre build
# Runs automatically during the pre build phase
set -euo pipefail

echo "[hook] pre build started..."
# TODO: add pre build logic
echo "[hook] pre build complete."
